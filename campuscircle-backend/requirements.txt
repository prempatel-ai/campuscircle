# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Settings / validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Database (Phase 2+, included now so Docker build doesn't need to change)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth (Phase 3+, included now for the same reason)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Email delivery
resend==2.6.0

# Dev/testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# YouTube Transcript Extraction
youtube-transcript-api==0.6.3
yt-dlp>=2024.1.1
